## Drony zespol 16

## Setup

- duplicate the folder ".secrets_example"
- change the name to ".secrets"
- fill all missing secrets
- then "docker compose up"

### Front Libraries

- axios <- sending http request, interceptors
- zustand <- state managment
