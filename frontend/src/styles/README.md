Module: styles

Purpose

- Global styles, variables, mixins, and design tokens.

Contents

- Base styles, CSS variables, global resets.

Conventions

- No component-specific styles; place them near components.
- Use tokens and variables for consistency.

Examples

- `globals.css`, `variables.css`, `mixins.css`

Importing

- `import '@/styles/globals.css'`
